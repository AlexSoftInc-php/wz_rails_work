<h1>Главная страница</h1>
<h2>Все наши новости (записи)</h2>

<% @post.each do |post| %>
	<div class="well">
		<h3><%= post.title %></h3>
		<p><%= post.body %></p>
		<% link_to "Смотреть подробно", posts_path(post), :class => 'btn btn-sm btn-primary' %>
	</div>
<% end %>